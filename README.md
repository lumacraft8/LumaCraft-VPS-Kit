# 💎 LumaCraft VPS Kit
Script profesional multi-lenguaje (ES/EN/FR) para setup de servidores.

## 🚀 Instalación Rápida
Ejecuta esto en tu VPS para instalar el comando globalmente:
```bash
wget [https://raw.githubusercontent.com/TU_USUARIO/LumaCraft-Kit/main/kit.sh](https://raw.githubusercontent.com/TU_USUARIO/LumaCraft-Kit/main/kit.sh) -O l-kit
chmod +x l-kit
sudo mv l-kit /usr/local/bin/
l-kit

---

### PASO 3: Cómo usarlo en tu VPS ahora mismo

Una vez subido a GitHub, para tener el comando `l-kit` funcionando, solo entra a tu VPS y escribe los comandos que pusimos en el README:

1.  **Descargar y renombrar:**
    `wget https://raw.githubusercontent.com/TU_USUARIO/LumaCraft-Kit/main/kit.sh -O l-kit`
2.  **Dar permisos:**
    `chmod +x l-kit`
3.  **Hacerlo global:**
    `sudo mv l-kit /usr/local/bin/`
4.  **Ejecutar:**
    Solo escribe `l-kit` y verás el selector de idiomas y tu arte ASCII.

---

### Explicación breve de lo que hicimos:
1.  **El Script:** Ahora es inteligente. Habla 3 idiomas y adapta sus preguntas y el resumen final.
2.  **El ASCII:** Se mantiene como tu firma visual al inicio y al final.
3.  **El Repositorio:** Es tu base de datos en la nube.
4.  **El Comando `l-kit`:** Al moverlo a `/usr/local/bin/`, has convertido un simple archivo en una herramienta del sistema.

¡Ya tienes un proyecto completo para mostrar en tu portfolio! ¿Hay algo más que quieras ajustar?
